name: CI

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:
    inputs:
      force_heavy:
        description: Run heavy plugin builds for manual dispatch
        required: false
        type: boolean
        default: true

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: EsionHsrahLatigid/juce-ci/.github/workflows/plugin-ci.yml@926c5bb10335c29503f92869169759ec18d71449
    permissions:
      contents: read
    with:
      product_name: IronPress
      product_slug: ironpress
      cmake_option_prefix: IRONPRESS
      debug_targets_json: '["ironpress_dsp_tests"]'
      release_test_targets_json: '["ironpress_dsp_tests","ironpress_plugin_tests","ironpress_editor_tests"]'
      windows_debug_plugins: false
